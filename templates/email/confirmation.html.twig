<h3>
    Hello {{ user.getName() }}
</h3>

<p>
    Please confirm your e-mail address by clicking
    <a href="{{ url('default_confirm_token', { 'token': user.confirmationToken() })}}">this</a>
    link or type the code inside the form: <b>{{ user.getConfirmationToken() }}</b>.
</p>